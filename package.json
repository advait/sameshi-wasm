{
  "name": "sameshi-wasm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "./scripts/build-wasm.sh",
    "prepare:web-wasm": "node ./scripts/prepare-web-wasm.mjs",
    "dev": "bun run prepare:web-wasm && vite",
    "build": "bun run prepare:web-wasm && vite build",
    "preview": "vite preview",
    "test": "bun test tests/*.test.ts",
    "test:e2e": "bun run prepare:web-wasm && playwright test"
  },
  "dependencies": {
    "chess.js": "^1.4.0",
    "chessground": "^9.2.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "vite": "^7.3.1"
  }
}
